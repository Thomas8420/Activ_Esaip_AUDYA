import React from 'react';
import HealthPage from '../../components/Health/HealthPage';

/**
 * Ecran Ma sante — conteneur mince.
 * La logique metier et l'UI resident dans HealthPage.
 */
const HealthScreen = () => {
  return <HealthPage />;
};

export default HealthScreen;

