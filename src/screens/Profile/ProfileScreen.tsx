import React from 'react';
import ProfilePage from '../../components/Profile/ProfilePage';

/**
 * Écran Mon Profil — conteneur mince.
 * La logique métier et l'UI résident dans ProfilePage.
 */
const ProfileScreen = () => {
  return <ProfilePage />;
};

export default ProfileScreen;
